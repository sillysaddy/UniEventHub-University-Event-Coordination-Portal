{
	"name": "campussync",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\""
	},
	"author": "FlexedPanda",
	"license": "MIT",
	"devDependencies": {
		"concurrently": "^9.1.0"
	}
}
